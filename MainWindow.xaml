<Window
    x:Class="MicGainControl.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicGainControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MicGainControl">

    <Grid Padding="24" RowSpacing="16" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon Glyph="&#xE720;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="MicGainControl" Style="{StaticResource TitleTextBlockStyle}" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="Изменения применяются сразу" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </StackPanel>

        <!-- Volume Section -->
        <StackPanel Grid.Row="1" Spacing="4">
            <Grid>
                <TextBlock Text="Громкость микрофона" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                <TextBlock x:Name="VolumeValueText" Text="100%" Style="{StaticResource BodyTextBlockStyle}" FontWeight="Bold" HorizontalAlignment="Right"/>
            </Grid>
            <Slider x:Name="VolumeSlider" Minimum="0" Maximum="100" StepFrequency="1" Value="100" ValueChanged="VolumeSlider_ValueChanged"/>
        </StackPanel>

        <!-- Enabled Toggle -->
        <Grid Grid.Row="2">
            <TextBlock Text="Микрофон" Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <ToggleSwitch x:Name="EnabledToggle" OnContent="Вкл" OffContent="Выкл" IsOn="True" Toggled="EnabledToggle_Toggled" HorizontalAlignment="Right"/>
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="4" VerticalAlignment="Bottom">
            <HyperlinkButton Content="GitHub" NavigateUri="https://github.com/megoRU/MicGainControl/releases" HorizontalAlignment="Left" Padding="0"/>
            <TextBlock Text="Версия 0.1.6" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
